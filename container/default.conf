map $http_accept $md_wanted {
    default            0;
    "~*text/markdown"  1;
}

# /a/b/ -> /a/b
map $uri $uri_base {
    "~^(?<p>.*)/$"  $p;
    default         $uri;
}

server {
    listen 8080;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    absolute_redirect off;

    error_page 404 /404.html;

    include /etc/nginx/security-headers.conf;
    add_header Cache-Control "public, max-age=0, must-revalidate";
    add_header Vary "Accept" always;
    add_header Link "</llms.txt>; rel=\"llms-txt\", </llms-full.txt>; rel=\"llms-full-txt\"" always;

    location /.well-known/security.txt {
        return 302 https://www.ncsc.nl/.well-known/security.txt;
    }

    # Bestanden met een contenthash in de naam kunnen nooit van inhoud
    # veranderen. Twee vormen: `naam.<md5>.ext` van resources.Fingerprint en
    # `naam_hu_<hash>.ext` van Hugo's afbeeldingsbewerking. Wijzigt Hugo dat
    # tweede patroon, dan vallen die afbeeldingen terug op de regel eronder.
    location ~ "(?:\.[0-9a-f]{32}|_hu_[0-9a-f]+)\.[^./]+$" {
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location ~* \.(png|jpe?g|gif|webp|svg|ico|woff2?)$ {
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "public, max-age=86400";
    }

    location ~ \.md$ {
        default_type text/markdown;
        charset utf-8;
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "public, max-age=0, must-revalidate";
        add_header X-Robots-Tag "noindex" always;
        add_header Vary "Accept" always;
        try_files $uri =404;
    }

    location / {
        # Serveer .md alleen bij Accept: text/markdown én als het bestand bestaat.
        set $serve_md "";
        if ($md_wanted) {
            set $serve_md "A";
        }
        if (-f "$document_root${uri_base}/index.md") {
            set $serve_md "${serve_md}B";
        }
        if ($serve_md = "AB") {
            rewrite ^ "${uri_base}/index.md" last;
        }

        try_files $uri $uri/ =404;
    }
}
