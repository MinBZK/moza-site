pre-commit:
  commands:
    hugo-build:
      run: rm -rf public && hugo --minify --logLevel warn
    htmltest:
      run: rm -rf tmp/public && hugo --minify --quiet --baseURL / --destination tmp/public && htmltest && rm -rf tmp/public
